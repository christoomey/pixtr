<h2>Images that <%= @user.email %> likes</h2>

<% @liked_images.each do |liked_image| %>
  <%= link_to gallery_image_path(liked_image.gallery, liked_image) do %>
    <%= image_tag liked_image.url %>
  <% end %>
<% end %>
